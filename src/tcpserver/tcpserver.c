#include <errno.h>
#include <stdio.h>
#include <sys/socket.h>
#include <netinet/ip.h>

void parse_ip(char *addrstr, struct sockaddr_in *addr) {
    
}

int main(void) {
    printf("hello world\n");
    return 0;
}
